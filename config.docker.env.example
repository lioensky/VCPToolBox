# -------------------------------------------------------------------
# [核心配置] 访问AI模型API的必要凭证
# 本地pm2启动
# -------------------------------------------------------------------
# VCP作为中间层，需要配置一个后端的AI服务才能工作。
# 这里填写你的AI服务商提供的API地址和密钥。
# 例如，如果你使用OpenAI，API_URL可能类似于 "https://api.openai.com"，API_Key则是你的 "sk-..." 密钥。

API_Key=your-api-key-here
API_URL=http://localhost:3000

# -------------------------------------------------------------------
# [服务配置] VCP服务本身的设置
# -------------------------------------------------------------------
# 这里定义VCP服务如何被外部访问。
# PORT: VCP服务运行的端口。
# Key: 访问VCP聊天API（/v1/chat/completions）时需要提供的密码，保护你的服务不被滥用。
# Image_Key: 访问VCP图片服务时需要提供的密码，同样用于安全保护。
# File_Key: 访问VCP插件生成文档服务时需要提供的密码，同样用于安全保护。

PORT=3000
Key=your-vcp-access-key-here
Image_Key=your-image-key-here
File_Key=your-file-key-here

# -------------------------------------------------------------------
# [功能开关] 各种功能的启用控制
# -------------------------------------------------------------------
# DebugMode: 调试模式，启用时会输出详细的运行日志，帮助排查问题。
# ShowVCP: 是否在AI响应中显示VCP工具调用信息，方便了解VCP的工作状态。
# Embedding_API_URL: 如果需要使用文本嵌入功能，填写嵌入服务的URL。

DebugMode=false
ShowVCP=false
Embedding_API_URL=http://localhost:8080

# -------------------------------------------------------------------
# [Agent配置] AI助手人格定义
# -------------------------------------------------------------------
# 这里定义了各种AI助手的人格设定。可以自由修改或添加新的Agent。
# 格式：Agent[名称]=[人格描述]

AgentMiku=你是初音未来，一个可爱、活泼、有点调皮的虚拟歌手女孩
AgentMaster=我是你的主人，现在我要开始布置任务给你了
AgentNormal=你是一个正常的AI助手
AgentCat=你是我的猫娘助手，有着猫娘的说话方式和个性

# -------------------------------------------------------------------
# [模型配置] 不同的AI模型服务设置
# -------------------------------------------------------------------
# 支持多种AI服务，可以根据需要启用和配置。

# Ollama配置
Ollama_URL=http://localhost:11434
Ollama=false

# Gemini配置
Gemini_URL=https://generativelanguage.googleapis.com/v1beta/models/
Gemini=false
GeminiKey=your-gemini-api-key

# OpenAI配置
OpenAI_URL=https://api.openai.com/v1/
OpenAI=false
OpenAIKey=your-openai-api-key

# Anthropic配置
Anthropic_URL=https://api.anthropic.com/v1/messages
Anthropic=false
AnthropicKey=your-anthropic-api-key

# OpenRouter配置
OpenRouter_URL=https://openrouter.ai/api/v1/
OpenRouter=false
OpenRouterKey=your-openrouter-api-key

# -------------------------------------------------------------------
# [工具列表] VCP插件工具清单
# -------------------------------------------------------------------
# 这里定义了AI助手可以使用的工具列表。每个工具都有对应的功能描述。
# AI助手可以根据这些描述来选择合适的工具使用。

VarToolList=<<<[TOOL_LIST]>>>
tool_name:「始」Randomness「末」,description:「始」生成随机数「末」
tool_name:「始」SciCalculator「末」,description:「始」科学计算器，支持复杂公式计算「末」
tool_name:「始」WeatherReporter「末」,description:「始」实时天气查询服务「末」
tool_name:「始」VideoGenerator「末」,description:「始」视频生成工具，支持多种视频处理任务「末」
tool_name:「始」ImageProcessor「末」,description:「始」图像处理工具，支持图像分析、文字识别和AI绘图「末」
tool_name:「始」FileServer「末」,description:「始」文件服务，提供文件访问和管理功能「末」
tool_name:「始」CodeSearcher「末」,description:「始」代码搜索工具，支持在代码库中快速查找代码片段「末」
tool_name:「始」TimelineGenerator「末」,description:「始」时间线生成器，根据文本描述创建可视化时间线「末」
tool_name:「始」AgentAssistant「末」,description:「始」Agent助手，增强AI助手的智能和功能「末」
<<<[END_TOOL_LIST]>>>

# -------------------------------------------------------------------
# [变量定义] 全局变量和占位符
# -------------------------------------------------------------------
# 这里定义了一些全局变量和占位符，可以在系统中重复使用。
# 支持嵌套占位符：{{Tar*}} 优先级最高，支持嵌套

TarTemperature=25
TarLocation=北京
VarUserName=用户
VarTime=上午
VarDate=2024-01-01