# 小红书 Cookie 配置
# 从浏览器 F12 -> Application -> Cookies -> xiaohongshu.com 中获取

# 必填：a1 (设备指纹 Cookie，约 52 位字符)
XHS_COOKIE_A1=

# 必填：web_session (登录态 Cookie，以 040069 开头)
XHS_COOKIE_WEB_SESSION=

# 推荐填写：webId (与 a1 配套的设备 ID)
XHS_COOKIE_WEB_ID=

# 可选：如果上面三项不够用，可以把完整 Cookie 字符串粘贴到这里（会覆盖上面三项）
XHS_COOKIE_FULL=

# 请求超时（秒）
REQUEST_TIMEOUT=20