# MultimediaProcessor 插件配置示例

# API 配置（如果不设置，将使用全局配置）
# API_URL=http://localhost:6005
# API_Key=password

# 多模态模型配置
MultiModalModel=gemini-2.5-flash-lite
MultiModalModelOutputMaxTokens=2000
MultiModalModelThinkingBudget=0
MultiModalModelAsynchronousLimit=3

# 支持的媒体格式（白名单，逗号分隔）
# 默认支持常见图片、视频、音频格式
SupportedMediaFormats=.jpg,.jpeg,.png,.gif,.webp,.bmp,.tiff,.mp4,.mov,.avi,.mp3,.wav,.ogg,.flac,.m4a

# 调试模式
DebugMode=false