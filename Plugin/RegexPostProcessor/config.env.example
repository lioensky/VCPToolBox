# RegexPostProcessor 插件配置文件示例

# 正则表达式规则文件路径（相对于插件目录）
# 默认值: "regex-rules.json"
# 可以指定其他JSON文件路径
RulesFilePath=regex-rules.json

# 是否自动重新加载规则文件（当文件被修改时）
# true: 启用自动重新加载
# false: 仅在插件初始化时加载一次
AutoReloadRules=false

# 自动重新加载规则文件的检查间隔（毫秒）
# 仅在AutoReloadRules=true时有效
# 建议值: 5000 (5秒)
ReloadInterval=5000

# 是否启用流式处理模式
# true: 启用流式处理（推荐）
# false: 仅使用非流式处理
EnableStreaming=true

# 流式处理时的chunk缓冲区大小（字符数）
# 较小的值可以更快处理，但可能增加系统负载
# 较大的值可以提高效率，但响应延迟会增加
ChunkBufferSize=1000

# 流式处理时的处理间隔（毫秒）
# 控制流式处理器的处理频率
# 较小的值可以更快响应，但可能增加CPU使用率
ProcessInterval=500

# 使用说明：
# 1. 正则表达式规则现在存储在 regex-rules.json 文件中
# 2. 插件会在初始化时自动加载规则文件
# 3. 可以通过API动态更新规则，更新后的规则会自动保存到文件
# 4. 规则文件格式请参考 regex-rules.json 文件