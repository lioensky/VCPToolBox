# DiscourseReader 配置文件
# 将此文件复制为 config.env 并填入你的配置
# ============================================================

# 默认论坛地址
DISCOURSE_BASE_URL=https://linux.do

# ListTopics 默认板块 slug（不指定 category 参数时使用）
DEFAULT_CATEGORY=develop

# 帖子内容最大字符数（超过截断）
MAX_CONTENT_LENGTH=3000

# ReadTopic 默认最大楼层数
MAX_POSTS=10

# ============================================================
# 认证配置（多论坛支持）
# ============================================================
#
# 【推荐】User API Key（永久有效）
# 格式：forum_{论坛名}_api_key=密钥
# {论坛名} 会与论坛域名进行模糊匹配
# 例如：forum_linuxdo_api_key 匹配 linux.do
#
# 获取方式：
#   pip install pycryptodomex
#   python get_user_api_key.py https://linux.do
#   按提示在浏览器授权，粘贴密文即可获得 Key
#
# forum_linuxdo_api_key=你的key
# forum_metadiscourse_api_key=另一个论坛的key

# ============================================================
# 【备选】Cookie 认证（session 级，可能过期）
# 配置了 API Key 时优先使用 API Key
# 格式：forum_{论坛名}_cookie=浏览器原始cookie字符串
#
# forum_linuxdo_cookie=_t=xxx; _forum_session=xxx