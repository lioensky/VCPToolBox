# 钉钉 AI 表格兼容层配置
#
# 本插件不再直接配置 MCP URL 或 MCP Key。
# 表格访问统一转发到 Plugin/DingTalkCLI，并受其 dry_run 与
# DWS_GRAY_STAGE 策略门控保护。

# 默认表格 UUID（可选）
DINGTALK_TABLE_UUID=

# 时区设置
DEFAULT_TIMEZONE=Asia/Shanghai

# 写入灰度策略请在 Plugin/DingTalkCLI/config.env 中配置：
# DWS_GRAY_STAGE=query_only
