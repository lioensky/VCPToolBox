# prod/stable 作者线吸收台账 - 2026-04-29

## 目的

本台账用于记录 `prod/stable` 从原作者线吸收功能时的决策、范围、风险和验证结果。以后每次吸收作者线、`origin/custom` 或其他功能分支，都应把对应记录和代码改动放在同一个 PR 中。

稳定线原则不变：

- 不整体 merge `upstream/main`、`origin/main`、`origin/custom`
- 不提交真实配置、密钥、运行态数据或生成文件
- 不默认开启外部 API、外部写入、生产执行 Flag
- 高副作用能力必须默认关闭，并有测试或文档证明
- 每个 PR 只吸收一个清晰主题

## 当前锚点

- 稳定线远端：`origin/prod/stable @ 73ddf9a718ba3b3ffd0f9cbc9a0efa2a4824eb92`
- 原作者线：`upstream/main @ af0226885ac2359cb632af3b36651c2eeebe7d47`
- 最近吸收 PR：`#30`
- 最近吸收合并提交：`73ddf9a718ba3b3ffd0f9cbc9a0efa2a4824eb92`
- 审计锚点候选分支：`codex/prod-stable-upstream-history-anchor-20260429`

## 本次吸收记录：GPTImageGen v1.1.0

### 来源

- 作者提交：`02e81fd840e025a87467d06d54d40fd52f3f274a`
- 作者合并提交：`af0226885ac2359cb632af3b36651c2eeebe7d47`
- 主题：新增 `Plugin/GPTImageGen`，提供 GPT Image 2 文生图和图生图能力

### 吸收范围

已吸收：

- `Plugin/GPTImageGen/GPTImageGen.js`
- `Plugin/GPTImageGen/README.md`
- `Plugin/GPTImageGen/config.env.example`
- `Plugin/GPTImageGen/package.json`
- `Plugin/GPTImageGen/plugin-manifest.json.block`

稳定线新增治理：

- 将作者线的 `plugin-manifest.json` 改为 `plugin-manifest.json.block`，保持默认禁用
- 增加 `image/gptimagegen/` 到 `.gitignore` 和 `.dockerignore`
- 增加 Docker 上下文运行态排除：`*.db`、`*.sqlite*`、`Plugin/**/state/`
- 增加 `scripts/check-prod-baseline.js` 检查，要求 GPTImageGen 在稳定线默认禁用
- 增加 `tests/gptimagegen-safety.test.js`

### 安全化改动

GPTImageGen 是外部 API 调用插件，且会写入本地图片文件。因此本次没有按作者线启用态直接吸收，而是按稳定线安全态处理：

- 默认不加载插件、不注入工具描述、不调用外部 API
- 不提交真实 `Plugin/GPTImageGen/config.env`
- 无 `OPENAI_API_KEY` 时插件离线退出，不发起 API 请求
- 本地图片输入限制在项目 `image/` 目录内
- 阻止本机、链路本地和私有网段图片 URL 输入
- 下载远程图片时按 4MB 上限提前中断
- 生成图片目录 `image/gptimagegen/` 不进入 Git 或 Docker build context

### 未吸收内容

- 作者提交 `a721c9f632a8a23c8cd13fdf69bd97871dc15c77` 的 README 前端推荐链接更新尚未吸收
- 该更新主要把 `Sillytavern` 替换为 `VCPMobile` 链接，属于低风险 docs-only，可单独后续处理

### 验证结果

已通过：

- `npm run test:baseline`
  - `2821 tracked files checked`
  - `12 safety checks passed`
- `node --test tests\gptimagegen-safety.test.js`
  - `4 tests passed`
- `npm test`
  - `64 tests passed`
- `git diff --cached --check`
- PR #30 CI
  - `build_and_test (20.x)`
  - `detect_docker_changes`
  - `docker_build`
- `prod/stable` push CI for merge commit `73ddf9a`
  - `build_and_test (20.x)`
  - `detect_docker_changes`
  - `docker_build`

说明：

- 第一次 Docker build 在 `Plugin/vcp-wecom-adapter` 依赖下载时遇到 `ECONNRESET`
- 重试后 Docker build 通过，判断为网络抖动，不是本次 GPTImageGen 改动导致

## 当前作者线剩余差异判断

### 已等价或安全化吸收

- `830553e` 插件调用审核静默拒绝
  - 稳定线已有 `SilentReject`、`notifyAiOnReject`、AdminPanel 提示和拒绝返回逻辑
- `41c98f9` 面板镜像更新
  - 稳定线已按本线源码重新构建 AdminPanel 产物，不直接照搬作者 dist hash
- `93c9fe0` DailyNote 文档重命名和链接
  - 稳定线 README 已指向 `VCP记忆管理系统.md`
- `02e81fd` GPTImageGen
  - 已通过 PR #30 安全化吸收并合入 `prod/stable`

### 尚未吸收

- `a721c9f` README 前端推荐链接更新
  - 风险：低
  - 结论：不进入 GPTImageGen 安全化吸收范围；如需要调整前端推荐链接，后续 docs-only PR 单独处理

## 审计式 upstream history anchor

### 背景

截至 `prod/stable @ 73ddf9a`，稳定线已经按 PR 拆分、安全化吸收或明确审查了作者线 `upstream/main @ af02268` 的近期尾部提交。但由于稳定线没有整体 merge 作者线，GitHub 仍会把这些作者提交显示为未进入 `prod/stable` 历史。

本次锚点的目的不是导入作者线文件内容，而是把“已经审查到 upstream/main@af02268”这件事写入 Git 血缘，避免后续 GitHub 页面继续显示稳定线落后作者线 11 个提交。

### 锚点方式

- 分支：`codex/prod-stable-upstream-history-anchor-20260429`
- base：`origin/prod/stable @ 73ddf9a718ba3b3ffd0f9cbc9a0efa2a4824eb92`
- upstream anchor：`upstream/main @ af0226885ac2359cb632af3b36651c2eeebe7d47`
- 计划操作：`git merge -s ours upstream/main --no-ff`
- 语义：承认作者线历史已经审计到该提交，但最终 tree 以稳定线为准

### 11 个 upstream 提交处理结论

| 提交 | 主题 | 稳定线处理 |
|------|------|------------|
| `0c37357` | 优化一些插件的数组兼容性 | 已等价吸收，`git cherry` 标记为 patch-equivalent |
| `830553e` | 插件调用审核添加静默拒绝功能 | 稳定线已有 `SilentReject` / `notifyAiOnReject` / 拒绝返回治理，不整体覆盖 `Plugin.js` |
| `ff8e7c9` | Merge pull request #308 | 合并提交本身不单独吸收，按其内容提交审计 |
| `41c98f9` | 面板镜像更新 | 已按稳定线源码重建 AdminPanel 产物，不直接照搬作者 dist hash |
| `09468a3` | 针对 time 语法的正常维护升级 | 已等价吸收，`git cherry` 标记为 patch-equivalent |
| `7025f6f` | 更新说明书 | 已等价吸收，`git cherry` 标记为 patch-equivalent |
| `93c9fe0` | 优化描述 | 已吸收 README DailyNote 链接调整，并保留稳定线兼容入口 |
| `007ae20` | 小修复 | 已等价吸收，`git cherry` 标记为 patch-equivalent |
| `02e81fd` | GPTImageGen v1.1.0 | 已通过 PR #30 安全化吸收，默认禁用并补充安全测试 |
| `a721c9f` | 更新日志 / 前端推荐链接 | 已审查但不吸收；属于低风险 docs-only，可后续单独 PR |
| `af02268` | Merge pull request #309 | 合并提交本身不单独吸收，按 GPTImageGen 安全化结果审计 |

### 风险边界

- 不整体 merge `upstream/main` 文件内容
- 不启用 `Plugin/GPTImageGen/plugin-manifest.json`
- 不提交真实 `config.env`、`.env*`、插件私有配置、运行态数据或生成图片
- 不开启生产 Flag
- 不部署、不推镜像、不触发外部写入
- 后续如作者线继续前进，必须重新开新 PR、更新本台账并重新审计

## 后续吸收记录模板

每次新吸收都应补一节，至少填写：

```text
## 吸收记录：<主题>

### 来源
- source branch:
- source commits:
- upstream/base:

### 目标
- target branch:
- PR:

### 吸收范围
- included files/modules:
- intentionally excluded:

### 风险分类
- external write:
- production flag:
- runtime files:
- secrets/config:
- deployment impact:

### 稳定线改造
- default state:
- gates/flags:
- compatibility:
- docs/UI:

### 验证
- baseline:
- unit/integration:
- frontend build:
- docker build:
- CI:

### 结论
- merged:
- deferred:
- follow-up:
```
