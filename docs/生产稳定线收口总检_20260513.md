# 生产稳定线收口总检 2026-05-13

本文记录 `prod/stable` 在吸收作者线 2026-05-13 更新后的收口状态，覆盖 PR #45-#51 的吸收结果、剩余拒绝/暂缓项、发布前安全状态和后续部署前检查门。

## 基线状态

- 当前稳定线锚点：`origin/prod/stable @ 66cc22c7b6fa9e9cb40333a29b839816db04118d`
- 当前吸收范围：来自 `docs/生产稳定线作者线更新审计_20260513.md` 中低风险和中风险建议优先处理项
- 本轮策略：逐项手工吸收、单独分支、单独 PR、单独 CI，不整体 merge 作者线
- 生产状态：未部署、未推镜像、未开启生产 Flag、未写外部服务

## 已合并吸收结果

| PR | 合并提交 | 吸收内容 | 安全结论 |
|---|---|---|---|
| #45 `chore: absorb low-risk upstream ignore rules` | `fc8bb3457f581ba221eff224dee85b382a2e410b` | 补入 `pluginStoreSources.json`、`/tmp/`、`Plugin/.backup/` 忽略规则 | 只增加本地缓存/临时目录忽略规则，不改变运行时行为 |
| #46 `fix: support GPTImageGen array string edit inputs` | `37c2bc3002a205ef1b1fad8cf60bde93dd2f4923` | GPTImageGen 支持 JSON array string 多图编辑输入 | 插件仍默认 `.block` 禁用，未放宽外部图像生成默认路径 |
| #47 `fix: harden VSearch network timeouts` | `ff7f4c4f7bbdeedf2c6ecc82d8f4870fc54269ae` | VSearch deadline、abort、重试和流式截断回退 | 只增强网络异常恢复，不新增外部写入 |
| #48 `fix: persist dynamic tool embedding vectors` | `6d1f008e64fb5e21e0f886aed9990b20cecf5dd7` | DynamicToolRegistry 持久化 embedding 索引接入，并修正原文向量语义 | 不绕过 approval / identity / effect classification |
| #49 `fix: remove unsafe multimodal prompt quotes` | `f5964c084b070bddfcd97f9390a4493c734a3b87` | 修复 `config.env.example` 中 `MultiModalPrompt` 内层引号 | 只改示例配置，不改真实 `config.env` |
| #50 `fix: hide stale distributed registry ids` | `2e0bf62cf6274553481c6d2fe0bc997bb3923d6d` | Admin 状态隐藏同一云插件重新上线后的旧离线 id | 只调整管理态可见性，不新增路由或权限绕行 |
| #51 `fix: limit VCPTavern access time logging` | `66cc22c7b6fa9e9cb40333a29b839816db04118d` | VCPTavern 仅在使用 last-chat 变量时记录访问时间和 session key | 默认更保守，减少不必要的时间和身份记录 |

## 验证矩阵

本轮代码 PR 均执行过本地验证，并通过 GitHub PR CI。

| 范围 | 固定验证 | 专项验证 |
|---|---|---|
| 基线安全 | `npm run test:baseline` | 禁止文件和稳定线安全门检查 |
| 全量测试 | `npm test` | 覆盖 ChannelHub、Codex memory、DynamicTools、Pipeline safety、System monitor、GPTImageGen、VSearch、VCPTavern |
| GPTImageGen | `tests/gptimagegen-safety.test.js` | JSON array string 输入先解析，再执行私有 URL 拒绝逻辑 |
| VSearch | `tests/vsearch-network-robustness.test.js` | deadline、AbortController、503 重试、流式截断 |
| DynamicTools | `tests/dynamicToolRegistry.test.js` | 持久化向量索引、原文 embedding 语义、旧分布式 id 隐藏 |
| VCPTavern | `tests/vcptavern-privacy.test.js` | 普通预设不写 access log，last-chat 预设才记录 session key |
| Docker CI | GitHub `docker_build` | #45-#51 均已通过或按 CI 规则完成门控 |

## 生产默认安全状态

- 未开启任何生产 Flag。
- 未部署到服务区。
- 未推送或发布 Docker 镜像。
- 未新增真实外部写入默认路径。
- 未提交真实配置：`config.env`、`.env*`、`Plugin/*/config.env` 未进入本轮吸收。
- 未提交运行态数据：`dailynote/`、`Plugin/*/state/`、`*.db`、`*.sqlite`、`image/` 未进入本轮吸收。
- GPTImageGen 仍保持默认禁用。
- Photo Studio 继续暂缓，本轮未吸收。

## 拒绝和暂缓项

以下内容不进入当前稳定生产线，除非后续另开评估或专项 PR。

| 项目 | 当前处理 | 原因 |
|---|---|---|
| 作者线 `.gitignore` 整体 diff | 拒绝整体照搬 | 会覆盖稳定线已有运行态、安全和构建边界忽略项 |
| README 广泛能力描述 | 暂缓 | 混有未吸收能力，容易造成文档过度承诺 |
| `Plugin/AgentAssistant/config.env.example` 新默认项 | 暂缓 | 应随 AgentAssistant base64 / 通信能力专项审查 |
| AdminPanel-Vue 大改 | 先评估 | 源码和 dist 同时变化，不能照搬作者线 dist hash |
| Browser control v3 / page-level CodeSearch | 先评估 | 涉及跨页面控制和权限边界 |
| 分布式异步兼容 / 异步插件占位符解析器 | 先评估 | 影响插件执行链路，需覆盖同步、异步、静态、拒绝和错误回退 |
| AgentAssistant base64 转发 | 默认不吸收 | 需先定义 payload 大小、MIME/用途边界、日志脱敏和默认关闭策略 |
| IP 追踪到 tool 端 | 默认不吸收 | 涉及隐私和日志留存，必须显式配置且默认关闭 |
| `20bd82f` 插件文件恢复为 upstream 版本 | 禁止 cherry-pick | 可能回退稳定线安全化改造 |
| 新图像插件和素材 | 暂缓 | SenseGen、NanoBananaGen2、ZImageTurboGen 等必须逐插件审计且默认禁用 |

## 发布前总检门

部署前仍需人工明确触发，并重新执行以下检查：

1. 确认代码锚点：`git rev-parse origin/prod/stable`
2. 确认工作树干净：`git status --short`
3. 重新跑基线：`npm run test:baseline`
4. 重新跑全量测试：`npm test`
5. 确认最近一次 PR CI `build_and_test (20.x)` 和 `docker_build` 全绿
6. 检查禁止文件未进入稳定线：`config.env`、`.env*`、`Plugin/*/config.env`、`Plugin/UserAuth/code.bin`、`dailynote/`、`Plugin/*/state/`、`*.db`、`*.sqlite`、`image/`
7. 仅从示例配置生成服务区真实配置，不从仓库提交或复制真实密钥
8. 部署前继续保持生产 Flag 默认关闭，只有明确部署指令后再进入服务区操作

## 收口结论

截至 `66cc22c7b6fa9e9cb40333a29b839816db04118d`，本轮作者线 2026-05-13 的低风险和中风险优先吸收项已收口。当前稳定生产线可以进入“部署前人工确认和环境预检”阶段，但尚未部署，也不应自动开启任何生产 Flag。

后续工作应以评估 PR 为主，优先处理高风险主题的边界说明，而不是继续扩大稳定线代码吸收范围。
