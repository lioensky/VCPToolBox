# prod/stable 作者线更新审计 - 2026-05-13

## 目的

本文件记录 `prod/stable` 对原作者线 `upstream/main` 新增更新的第一轮审计结论。此 PR 只建立审计台账，不吸收作者线代码，不做历史锚点，不部署，不开启生产 Flag，不触发外部写入。

稳定线原则不变：

- 不整体 merge `upstream/main`、`origin/main`、`origin/custom`
- 不提交真实配置、密钥、运行态数据或生成文件
- 不默认开启外部 API、外部写入、生产执行 Flag
- 高副作用能力必须默认关闭，并有测试或文档证明
- 后续吸收必须按主题拆分 PR，每个 PR 附带本台账更新

## 当前锚点

- 稳定线远端：`origin/prod/stable @ 85e83c9d8fc884c91ccf63a3e882407176377dfd`
- 上次作者线审计锚点：`upstream/main @ af0226885ac2359cb632af3b36651c2eeebe7d47`
- 本次作者线目标：`upstream/main @ 0086b15b53f00bcc841901f378110c182abc2d26`
- 本次审计范围：`af0226885ac2359cb632af3b36651c2eeebe7d47..0086b15b53f00bcc841901f378110c182abc2d26`

## 总体规模

- 新增作者线提交：54 个
- 文件变化规模：425 files changed, 31490 insertions(+), 5062 deletions(-)
- 主要目录分布：

| 区域 | 文件数 | 初步判断 |
|------|--------|----------|
| `AdminPanel-Vue/` | 302 | 前端源码和构建产物大改，必须独立审查和本线重建 |
| `image/` / 中文图片资源目录 | 66 | 运行/素材边界不清，默认不吸收 |
| `Plugin/` | 28 | 涉及多插件新增和行为修改，需要逐插件审查 |
| `routes/` | 10 | 管理端路由变化，需结合权限和生产 Flag 审查 |
| `modules/` | 4 | 核心运行时变化，需专项测试 |
| 根层核心文件 | 10+ | `Plugin.js`、`WebSocketServer.js`、`server.js` 等，不能整块覆盖 |

## 新增提交清单

| 提交 | 主题 | 初步分组 |
|------|------|----------|
| `5b32d26` | feat: overhaul admin panel and admin tooling | AdminPanel |
| `5960294` | feat(DailyNote): unified diff format for fuzzy match failure reporting | DailyNote |
| `613339b` / `7bef3bf` / `f4bdc38` | 为 ZImageTurboGen 增加网络代理和公网适配开关 | Image plugin |
| `b2dbc4b` | 引入一个建库探针 | RAG / database probe |
| `a3adf27` | 优化日志，引入工具路径追踪 | Runtime observability |
| `9a01f91` | 添加 ip 追踪精准到 tool 端 | Runtime / privacy-sensitive |
| `fc92a6b` | 全面升级 VCP 浏览器控制协议到 v3 | Browser bridge |
| `7693f7c` | 新增分布式异步兼容 | Distributed runtime |
| `65eca3b` | 优化更鲁棒的异步插件占位符解析器 | Async placeholder parser |
| `621ea94` | 新增商汤文生图 | SenseGen image plugin |
| `34029df` | 浏览器控制引入页面级 CodeSearch | Browser / CodeSearch |
| `4d075e9` | NanoBananaGen2 支持自定义渠道与多模型轮询 | Image plugin |
| `29eba40` | ObsidianBridge CLI 路径配置化 | ObsidianBridge |
| `9a974c0` | GPTImageGen support JSON array string for multi-image edit input | GPTImageGen |
| `1905a60` | 上架深度调研 Agent 微明 | Agent prompt |
| `3f779ad` | 对后端数据库优化一些建构性索引自查和防御 | Database hardening |
| `51d25df` | 优化网络波动的防御性检查 | Runtime robustness |
| `20bd82f` | revert: 插件文件恢复为 upstream 版本 | High-risk revert |
| `5940db9` | gitignore 新增 SQLite 数据库文件和动态工具配置缓存 | Ignore / baseline |
| `533d63a` | VCPTavern 不使用智能时间感知时，不记录时间和身份 | VCPTavern privacy |
| `1c5bd30` | 更新依赖和构建 | Dependency / build |
| `3c83a4f` | 修复全局动态占位符缺乏持久化索引的 bug | Dynamic placeholders |
| `11e6a73` | 修复云插件注册 id 重定向路由问题 | Cloud plugin registry |
| `b33a6a8` | 修复引号引发的提示词失效 | Prompt handling |
| `c1d120f` | 优化 VSearch 网络波动适应性 | VSearch |
| `0af0191` | 修复页面丢失任务中心 | AdminPanel |
| `c3da8c9` | 优化配置文件 | Config |
| `0086b15` | 允许 AgentAssistant 插件转发 base64 数据，兼容 river 语法 | AgentAssistant / data forwarding |

合并提交和重复修复提交保留在审计范围内，但后续吸收应按其实际内容归类，不单独按 merge commit 吸收。

## 禁入和高风险文件

本次作者线包含以下稳定线禁入或高风险项，不能直接吸收：

| 路径 | 风险 |
|------|------|
| `AdminPanel-Vue/.claude/settings.local.json` | 本地工具配置，不应进入稳定线 |
| `Plugin/DailyNote/config.env` | 真实/运行配置风险，禁止提交 |
| `Plugin/UserAuth/code.bin` | 用户认证运行态，禁止提交 |
| `image/*` / 中文图片资源目录 | 素材/运行态边界不清，默认不吸收 |
| `package.json` / `package-lock.json` | 依赖变更需独立验证，不随功能混入 |
| `Plugin/*/config.env.example` | 模板可审查，但必须确认无真实密钥和默认外部写入 |

## 主题分组和处理建议

### A. 低风险可优先评估

1. `.gitignore` / SQLite / 动态工具缓存忽略规则
   - 来源：`5940db9`
   - 建议：与稳定线 `scripts/check-prod-baseline.js` 对照后单独 PR

2. README / 描述修复 / 示例修复
   - 来源：`b5c5615`、`9af7d82`、`8cdbf15`
   - 建议：docs-only PR，避免混入 dist 或运行态

3. GPTImageGen JSON array string 输入兼容
   - 来源：`9a974c0`
   - 建议：基于稳定线已禁用安全化版本手工移植，并扩展 `tests/gptimagegen-safety.test.js`

### B. 中风险，需要专项测试

1. VSearch 网络波动适应性
   - 来源：`c1d120f`
   - 风险：影响检索稳定性和错误路径
   - 建议：新增 VSearch 断网/重试/降级测试后吸收

2. 动态占位符持久化索引
   - 来源：`3c83a4f`
   - 风险：影响提示词注入和动态工具目录
   - 建议：结合稳定线 DynamicTools 治理测试单独吸收

3. 云插件注册 id 重定向路由
   - 来源：`11e6a73`
   - 风险：涉及路由和插件注册身份
   - 建议：先确认不会绕过稳定线 approval / identity / effect classification

4. VCPTavern 时间和身份记录调整
   - 来源：`533d63a`
   - 风险：隐私和行为兼容
   - 建议：可作为 privacy fix 单独 PR

### C. 高风险，默认先评估不进代码

1. AdminPanel-Vue 大改
   - 来源：`5b32d26`、`c81e37f`、`8a5dce2`、`0af0191`
   - 风险：302 个文件，源码和 dist 同时变化
   - 建议：不要照搬 dist；若吸收，必须在稳定线源码上重新 `npm --prefix AdminPanel-Vue ci && npm --prefix AdminPanel-Vue run build`

2. Browser control v3 / page-level CodeSearch
   - 来源：`fc92a6b`、`34029df`
   - 风险：浏览器控制、跨页面能力、潜在外部行为
   - 建议：先写协议差异评估，确认权限边界再进代码

3. 分布式异步兼容 / 异步插件占位符解析器
   - 来源：`7693f7c`、`65eca3b`
   - 风险：影响插件执行链路和占位符解析
   - 建议：必须覆盖同步、异步、静态、拒绝、错误回退测试

4. AgentAssistant base64 转发
   - 来源：`0086b15`
   - 风险：base64 数据可能放大 payload、绕过文本审计或携带敏感内容
   - 建议：必须先定义大小限制、MIME/用途边界、日志脱敏和默认关闭策略

5. IP 追踪到 tool 端
   - 来源：`9a01f91`
   - 风险：隐私和日志留存
   - 建议：不默认吸收；如需要，必须有显式配置、日志脱敏和文档说明

6. 插件文件恢复为 upstream 版本
   - 来源：`20bd82f`
   - 风险：可能回退稳定线安全化改造
   - 建议：禁止 cherry-pick；只逐文件 diff 审查

### D. 新插件 / 图像能力，必须默认禁用

| 插件 | 来源 | 初步要求 |
|------|------|----------|
| `Plugin/SenseGen` | `621ea94` | 新增外部图像生成能力，必须 `.block` 默认禁用 |
| `Plugin/NanoBananaGen2` | `4d075e9` | 自定义渠道和多模型轮询需配置审计，默认禁用 |
| `Plugin/ObsidianBridge` | `29eba40` | 本地 CLI 路径配置化，需确认不会默认写本地库 |
| `Plugin/ZImageTurboGen` | `613339b` / `7bef3bf` / `f4bdc38` | 代理和公网适配开关需默认关闭 |
| `Plugin/SkillBridge` | `f149e58` | 需确认 bridge 权限和执行边界 |

## 推荐后续 PR 顺序

1. `codex/prod-stable-upstream-ignore-docs-20260513`
   - `.gitignore`、docs、README、示例修复

2. `codex/prod-stable-gptimagegen-array-compat-20260513`
   - 仅吸收 GPTImageGen multi-image JSON array string 兼容

3. `codex/prod-stable-vsearch-placeholder-fixes-20260513`
   - VSearch 网络波动、动态占位符持久化索引、引号提示词修复

4. `codex/prod-stable-plugin-registry-route-audit-20260513`
   - 云插件注册 id 重定向路由、插件身份边界

5. `codex/prod-stable-adminpanel-audit-20260513`
   - AdminPanel-Vue 源码差异审查，本线重建 dist

6. `codex/prod-stable-browser-v3-audit-20260513`
   - Browser control v3 和 page-level CodeSearch 协议评估

7. `codex/prod-stable-new-image-plugins-audit-20260513`
   - SenseGen / NanoBananaGen2 / ZImageTurboGen 默认禁用审计

## 当前结论

本次作者线更新规模大，且包含配置、运行态、图片素材、插件恢复、浏览器控制和外部图像能力等高风险内容。稳定线应继续采用“审计先行、按主题手工吸收、默认安全关闭”的路线。

本 PR 只建立 2026-05-13 作者线更新审计台账。后续任何代码吸收都必须另开 PR，并在本文件追加处理结果。

## 后续处理记录

### 2026-05-13 第一批低风险忽略规则 / 文档示例审查

- 分支：`codex/prod-stable-upstream-ignore-docs-20260513`
- 来源提交：`5940db9`、`8cdbf15`、`b5c5615`、`9af7d82`、`c3da8c9`
- 已吸收：
  - 手工补入插件商店本地运行/缓存文件忽略规则：`pluginStoreSources.json`、`/tmp/`、`Plugin/.backup/`
- 未吸收：
  - 未照搬作者线 `.gitignore` 整体 diff，因为该 diff 会删除稳定线已有的运行态、安全和构建边界忽略项。
  - 未吸收 README 变更，因为其中混有未在稳定线完成吸收的新能力描述，容易造成文档过度承诺。
  - 未吸收 `Plugin/AgentAssistant/config.env.example` 新默认项，因为其会改变插件通信提示词和超时行为，应随 AgentAssistant base64 / 通信能力专项一并审查。
- 安全结论：本批只增加本地缓存/临时目录忽略规则，不改变运行时行为、不开启生产 Flag、不引入外部写入。

### 2026-05-13 GPTImageGen JSON 数组字符串兼容

- 分支：`codex/prod-stable-gptimagegen-array-compat-20260513`
- 来源提交：`9a974c0`
- 已吸收：
  - 手工补入 `GPTEditImage` 对 JSON 数组字符串图片输入的兼容解析，支持 VCP 工具参数将 `["a","b"]` 作为字符串传入的情况。
  - 保留稳定线安全边界：插件仍为 `plugin-manifest.json.block` 默认禁用，本地图片读取仍限制在项目 `image/` 目录，私有/本机 URL 仍在网络访问前拒绝。
  - 更新 GPTImageGen README 和被禁用 manifest 文案，说明多图 JSON 数组字符串用法。
  - 增加回归测试，确认 JSON 数组字符串会先解析再执行私有 URL 拒绝逻辑。
- 安全结论：本批只提升已禁用插件的输入兼容性，不启用插件、不新增外部写入默认路径。

### 2026-05-13 VSearch 网络波动适应性

- 分支：`codex/prod-stable-vsearch-network-robustness-20260513`
- 来源提交：`c1d120f`
- 已吸收：
  - 手工补入插件内部安全截止时间：从 `plugin-manifest.json` 的 `communication.timeout` 派生 deadline，并预留安全回复余量。
  - Grounding 模式请求支持 `AbortController`，到达安全截止时间时中止未完成批次并返回已完成结果。
  - Grok 模式对 503 / 空响应做有限指数退避重试，并在安全截止时间前截断流式输出返回已收到内容。
  - 增加 `tests/vsearch-network-robustness.test.js`，锁定 deadline、abort、重试和截断回退保护。
- 安全结论：本批只增强网络异常和超时下的可恢复性，不新增外部服务、不改变默认搜索模式、不放宽外部写入边界。

### 2026-05-13 动态占位符持久化索引

- 分支：`codex/prod-stable-dynamic-placeholder-index-20260513`
- 来源提交：`3c83a4f`
- 已吸收：
  - 手工补入 DynamicToolRegistry 的 RAG embedding provider 优先级：优先使用 `getSingleEmbeddingCached`，再回退到 `getSingleEmbedding` / context bridge。
  - 当 `vectorDBManager.getPluginDescriptionVector` 可用时，动态工具分类 embedding 会以原始分类文本走持久化插件描述向量索引，避免改变向量语义或拆出不兼容缓存空间。
  - 保留稳定线兼容路径：`getContextBridge().embedText` 和旧 `getContextBridge().getSingleEmbedding` 均可作为 fallback。
  - 增加回归测试，确认动态工具分类会使用持久化插件描述向量，并且优先调用 cached embedding。
- 安全结论：本批只改善动态工具分类索引复用，不改变工具可用性、不绕过 approval / identity / effect classification 治理。

### 2026-05-13 多模态提示词引号修复

- 分支：`codex/prod-stable-prompt-quote-fix-20260513`
- 来源提交：`b33a6a8`
- 已吸收：
  - 手工修复 `config.env.example` 中 `MultiModalPrompt` 示例值的内层双引号，避免 `.env` 解析时提示词被截断或失效。
- 安全结论：本批只修改示例配置文本，不修改真实 `config.env`、不新增配置项、不改变默认外部调用路径。

### 2026-05-13 云插件注册 id 重定向路由修复

- 分支：`codex/prod-stable-plugin-registry-route-20260513`
- 来源提交：`11e6a73`
- 已吸收：
  - 手工补入 DynamicToolRegistry 管理态记录过滤：当同一分布式/云插件以新 id 重新上线时，Admin 状态只展示当前可用记录，不再展示同身份旧离线 id。
  - 旧离线记录仍保留在内部 catalog 中，用于历史状态保留，不改变工具执行、approval、identity 或 effect classification 边界。
  - 增加回归测试，确认同一云工具更换 server id 后 Admin 状态隐藏旧 id，但内部 catalog 仍保留旧离线记录。
- 安全结论：本批只调整管理态展示/重定向可见性，不新增路由、不放宽鉴权、不改变插件注册或执行权限。

### 2026-05-13 VCPTavern 访问时间与身份隐私修复

- 分支：`codex/prod-stable-vcptavern-privacy-time-identity-20260513`
- 来源提交：`533d63a`
- 已吸收：
  - 手工补入 VCPTavern 预设扫描：只有启用规则实际使用 `{{LastChatTime}}` 或 `{{TimeSinceLastChat}}` 时，才计算 session key 并写入访问时间日志。
  - 不使用访问时间变量的预设仍可解析 `{{Date}}` / `{{Time}}` / `{{Today}}`，但不会记录时间和身份。
  - 增加回归测试，确认普通预设不写 access log，使用 last-chat 变量的预设才写入对应 session key。
- 安全结论：本批默认更保守，减少不必要的时间和身份记录，不新增外部写入、不改变 VCPTavern 触发方式。
